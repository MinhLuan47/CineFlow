# Cấu hình Server (Server Configuration)
PORT=5000
NODE_ENV=development

# Cấu hình CORS (CORS Configuration)
# Cho phép frontend truy cập API (Vite mặc định chạy ở port 5173)
CORS_ORIGIN=http://localhost:5173
